# Realtime backend. Set ONE of these.
#
# PartyKit (default): the host of your `partykit deploy` output.
NEXT_PUBLIC_PARTYKIT_HOST=

# socket.io: the Render service URL from render.yaml. Leave
# NEXT_PUBLIC_PARTYKIT_HOST blank to use this path instead.
NEXT_PUBLIC_REALTIME_URL=
